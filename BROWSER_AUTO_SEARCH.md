# 🌐 浏览器自动搜索功能 - 使用指南

## ✨ 新功能说明

现在你的 AI Agent 支持**真正打开浏览器进行搜索**了！

### 工作原理

1. **前端页面**: 美观的 Web 界面
2. **智能判断**: 勾选"自动打开浏览器"选项
3. **双重搜索**: 
   - ✅ 在浏览器中打开 Bing/Google 搜索页面
   - ✅ 同时调用 API 获取智能回答
4. **最佳体验**: 既有即时回答，又能查看详细网页

---

## 🚀 快速开始

### 方式 1: 使用 Web 界面（推荐）⭐

#### Step 1: 启动服务

```bash
# Windows
go run cmd/server/main.go

# 或编译后运行
./build/server.exe
```

#### Step 2: 打开浏览器

访问: **http://localhost:8080**

#### Step 3: 使用功能

1. 输入问题（例如："原神 雷电将军怎么培养"）
2. ✅ 勾选 **"自动在浏览器中打开搜索结果"**
3. 点击 "🔍 搜索攻略"

**效果**:
- 🌐 自动在新标签页打开 Bing 搜索结果
- 💬 同时在页面显示智能整合的回答
- 🔗 提供"在浏览器中查看"链接

---

### 方式 2: 直接 API 调用

如果你只想在代码中使用：

```bash
curl -X POST http://localhost:8080/api/v1/smart-chat \
  -H "Content-Type: application/json" \
  -d '{"message": "黑神话悟空 虎先锋怎么打"}'
```

然后手动在浏览器打开：
```
https://www.bing.com/search?q=黑神话悟空+虎先锋怎么打+攻略
```

---

## 🎨 Web 界面功能

### 主要特性

✅ **美观的 UI**
- 现代化设计
- 响应式布局
- 流畅动画

✅ **智能选项**
- [x] 自动在浏览器中打开搜索结果
- [x] 使用智能搜索（工具调用）

✅ **快捷示例**
- 点击标签快速填入常见问题
- 原神、黑神话、塞尔达等热门游戏

✅ **实时状态**
- 显示服务器连接状态
- 每 10 秒自动检测

✅ **加载提示**
- 搜索时显示加载动画
- 友好的等待提示

---

## 📸 使用流程

```
用户输入问题
     ↓
勾选"自动打开浏览器"
     ↓
点击搜索按钮
     ↓
┌────────────────────────────┐
│ 1. 打开新标签页             │
│    → Bing 搜索结果页面      │
│                            │
│ 2. 当前页面显示             │
│    → 智能整合的回答         │
│    → 可点击的链接           │
└────────────────────────────┘
     ↓
用户可以：
- 查看智能回答（快速）
- 切换到浏览器标签（详细）
- 点击链接查看更多
```

---

## ⚙️ 配置选项

### 1. 自动打开浏览器

**勾选**: 
- ✅ 每次搜索都会自动打开新标签页
- ✅ 适合需要查看详细网页的场景

**不勾选**:
- ❌ 只在页面内显示智能回答
- ❌ 适合快速获取答案

---

### 2. 使用智能搜索

**勾选** (推荐):
- ✅ 调用 `/api/v1/smart-chat`
- ✅ 自动识别游戏问题
- ✅ 调用搜索工具
- ✅ 整合多个来源

**不勾选**:
- ❌ 调用 `/api/v1/simple-chat`
- ❌ 直接使用 LLM 回答
- ❌ 不调用搜索工具

---

## 🔧 自定义配置

### 修改默认搜索引擎

编辑 `web/index.html`，找到这一行：

```javascript
const searchUrl = `https://www.bing.com/search?q=${encodeURIComponent(question + ' 攻略')}`;
```

改为 Google:
```javascript
const searchUrl = `https://www.google.com/search?q=${encodeURIComponent(question + ' 攻略')}`;
```

改为百度:
```javascript
const searchUrl = `https://www.baidu.com/s?wd=${encodeURIComponent(question + ' 攻略')}`;
```

---

### 修改 API 地址

如果服务器不在本地，修改：

```javascript
const API_BASE = 'http://localhost:8080';
```

改为你的服务器地址：
```javascript
const API_BASE = 'http://your-server.com:8080';
```

---

## 💡 使用技巧

### 技巧 1: 快捷键

- **Enter 键**: 快速提交搜索
- **点击示例标签**: 快速填入问题

### 技巧 2: 多标签浏览

1. 勾选"自动打开浏览器"
2. 连续搜索多个问题
3. 每个问题都会打开新标签页
4. 可以对比不同来源的信息

### 技巧 3: 智能 + 手动结合

1. 先查看页面的智能回答（快速了解）
2. 再切换到浏览器标签（深入阅读）
3. 点击具体链接（查看原文）

---

## 🎯 典型场景

### 场景 1: 快速查询

**需求**: 想知道雷电将军的培养方法

**操作**:
1. 输入"原神 雷电将军怎么培养"
2. ✅ 勾选"自动打开浏览器"
3. 点击搜索

**结果**:
- 页面立即显示智能整合的培养指南
- 浏览器打开 Bing 搜索结果（10+ 个网页）
- 可以快速浏览或深入阅读

---

### 场景 2: 对比信息

**需求**: 想看看不同网站的攻略

**操作**:
1. 搜索问题
2. 在打开的多个标签页中切换
3. 对比不同来源的信息

**优势**:
- 避免单一来源的偏见
- 获取更全面的信息

---

### 场景 3: 保存资料

**需求**: 想保存有用的攻略链接

**操作**:
1. 查看智能回答
2. 从浏览器标签中找到有价值的网页
3. 收藏或复制链接

---

## 🐛 常见问题

### Q1: 浏览器没有自动打开？

**检查**:
1. 是否勾选了"自动在浏览器中打开搜索结果"
2. 浏览器是否阻止了弹窗
3. 查看浏览器地址栏是否有拦截提示

**解决**:
- 允许 localhost:8080 的弹窗
- 或手动点击"在浏览器中查看"链接

---

### Q2: 页面显示"服务器未连接"？

**原因**: 后端服务未启动

**解决**:
```bash
# 启动服务
go run cmd/server/main.go
```

---

### Q3: 搜索结果为空？

**可能原因**:
1. Bing API Key 未配置
2. 网络连接问题
3. 问题太冷门

**解决**:
- 检查环境变量 `BING_SEARCH_API_KEY`
- 尝试其他问题
- 查看日志 `logs/agent.log`

---

### Q4: 如何关闭自动打开浏览器？

**方法**:
- 取消勾选"自动在浏览器中打开搜索结果"
- 或修改 HTML，移除 `checked` 属性

---

## 📊 技术实现

### 前端架构

```
web/index.html
├── HTML 结构
├── CSS 样式（内联）
└── JavaScript 逻辑
    ├── checkConnection() - 检查服务器
    ├── handleSearch() - 处理搜索
    ├── displayResult() - 显示结果
    └── showError() - 错误处理
```

### 工作流程

```javascript
// 1. 构建搜索 URL
const searchUrl = `https://www.bing.com/search?q=${query}`;

// 2. 如果勾选了自动打开
if (autoOpen) {
    window.open(searchUrl, '_blank'); // 打开新标签
}

// 3. 同时调用 API
fetch('/api/v1/smart-chat', {
    method: 'POST',
    body: JSON.stringify({ message: question })
})
.then(response => response.json())
.then(data => {
    // 4. 显示智能回答
    displayResult(data);
});
```

### 后端支持

```go
// Gin 路由
router.Static("/web", "./web")  // 静态文件
router.GET("/", ...)             // 首页
router.POST("/api/v1/smart-chat", ...) // API
```

---

## 🎨 界面预览

```
┌────────────────────────────────────────────┐
│       🎮 游戏攻略智能体                     │
│   智能搜索最新攻略，自动打开浏览器查看        │
├────────────────────────────────────────────┤
│                                            │
│  [原神 雷电将军怎么培养............] [🔍]  │
│                                            │
│  ☑ 自动在浏览器中打开搜索结果               │
│  ☑ 使用智能搜索（推荐）                     │
│                                            │
├────────────────────────────────────────────┤
│                                            │
│  📋 搜索结果                               │
│                                            │
│  [🔗 在浏览器中查看]                        │
│                                            │
│  🎮 **原神 - 雷电将军怎么培养** 攻略搜索... │
│                                            │
│  找到 8 个相关结果:                         │
│                                            │
│  1. **雷电将军全面培养指南**                │
│     雷电将军是5星雷元素角色...              │
│                                            │
│  ...                                       │
│                                            │
├────────────────────────────────────────────┤
│  💡 试试这些问题：                          │
│  [原神 雷电将军] [黑神话 虎先锋] [...]     │
│                                            │
├────────────────────────────────────────────┤
│         ✅ 服务器已连接                      │
└────────────────────────────────────────────┘
```

---

## 🚀 部署建议

### 开发环境

```bash
# 本地运行
go run cmd/server/main.go

# 访问
http://localhost:8080
```

---

### 生产环境

1. **编译**
```bash
go build -o server cmd/server/main.go
```

2. **配置 Nginx**
```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

3. **启用 HTTPS**
```bash
# 使用 Let's Encrypt
certbot --nginx -d your-domain.com
```

---

## 📈 性能优化

### 前端优化

- ✅ CSS 内联（减少请求）
- ✅ 无外部依赖（加载快）
- ✅ 异步请求（不阻塞）

### 后端优化

- ✅ 静态文件缓存
- ✅ Gzip 压缩
- ✅ 连接池

---

## 🔮 未来扩展

### 可能的改进

- [ ] 搜索历史记录
- [ ] 收藏夹功能
- [ ] 分享搜索结果
- [ ] 多搜索引擎切换
- [ ] 搜索结果预览
- [ ] 深色模式

---

## 📞 技术支持

如有问题：

1. 📖 查看 [GAME_GUIDE_AGENT.md](GAME_GUIDE_AGENT.md)
2. 💬 提交 Issue
3. 🔍 检查浏览器控制台错误

---

## 🎉 开始使用！

现在你可以：

1. **启动服务**
```bash
go run cmd/server/main.go
```

2. **打开浏览器**
```
http://localhost:8080
```

3. **体验自动搜索**
- 输入问题
- 勾选"自动打开浏览器"
- 点击搜索
- 享受双重搜索体验！

**祝你使用愉快！** 🎮✨
